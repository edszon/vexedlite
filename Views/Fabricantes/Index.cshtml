@model IEnumerable<vxdlite.Models.Fabricante>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FabricanteNome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FabricanteNome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FabricanteID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FabricanteID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FabricanteID })
        </td>
    </tr>
}

</table>
